kind = "web"
previewPath = "/"
title = "BestUGCs - Roblox UGC Community"
version = "1.0.0"
id = "artifacts/bestugcs"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23379

[services.development]
run = "pnpm --filter @workspace/bestugcs run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bestugcs", "run", "build" ]
publicDir = "artifacts/bestugcs/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23379"
BASE_PATH = "/"
